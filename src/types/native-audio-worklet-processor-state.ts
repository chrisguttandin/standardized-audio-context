import { TAudioWorkletProcessorState } from './audio-worklet-processor-state';

// @todo Since there are no native types yet, both types are the same.
export type TNativeAudioWorkletProcessorState = TAudioWorkletProcessorState;
