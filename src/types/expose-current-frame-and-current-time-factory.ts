import { TExposeCurrentFrameAndCurrentTimeFunction } from './expose-current-frame-and-current-time-function';

export type TExposeCurrentFrameAndCurrentTimeFactory = (window: null | Window) => TExposeCurrentFrameAndCurrentTimeFunction;
